;8051 ALP TO FIND THE NUMBER OF ODD NUMBERS AND EVEN NUMBERS IN A GIVEN LIST

;INITIALIZE THE LIST
MOV 20H, #01H
MOV 21H, #02H
MOV 22H, #03H
MOV 23H, #04H
MOV 24H, #05H

MOV R0, #20H	;BASE ADDRESS
MOV R1, #05H	;COUNT
MOV R5, #00H	;FOR ODD COUNT
MOV R6, #00H	;FOR EVEN COUNT

;FIND ODD OR EVEN

HERE:
MOV A, @R0
ANL A, #01H	;AND WITH 01H
JZ NEXT			;IF ZERO, THEN EVEN
INC R5			;ELSE ODD

NEXT:
INC R0			;POINT TO NEXT NUMBER
DJNZ R1, HERE

MOV A, #05H	;EVEN NOS. = LIST COUNT - ODD NOS.
MOV B, R5
CLR C
SUBB A, B
MOV R6, A

HALT:
SJMP HALT		;HALT THE PROGRAM
