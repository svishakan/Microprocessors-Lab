;BLOCK TRANSFER OF 10 BYTES

ORG 0

START:
	MOV R0, #20H	;SOURCE POINTER
	MOV R1, #30H	;DESTINATION POINTER
	MOV R2, #0AH	;COUNT OF 10

BACK:
	MOV A, @R0		;FROM RO TO A
	MOV @R1, A		;FROM A TO R1
	INC R0
	INC R1
	DJNZ R2, BACK	;DECREMENT R2; CHECK IF R2 IS NOT ZERO, LOOP

HERE:
	SJMP HERE